
@if (User.Identity.IsAuthenticated)
{
 <span class="autenticacion">@User.Identity.Name, Sesión Iniciada <a asp-page="/Seguridad/logout">Cerrar Sesión</a></span>   
}
else
{
 <span class="autenticacion">Usuario no autenticado <a asp-page="/Seguridad/login">Iniciar Sesión</a></span>   
}
